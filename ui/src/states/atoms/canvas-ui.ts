import { atom } from 'jotai';

export const canvasUiMouseoverEdgeIdAtom = atom<string | null>(null);

export const canvasUiMouseoverNodeIdAtom = atom<string | null>(null);

export const canvasUiLeftMenuBarOpenAtom = atom<boolean>(false);
